sudo: false
dist: trusty
language: rust
matrix:
  include:
    - rust: stable
script:
  - ./test/build_openssl.sh
  - cargo update
  - cargo build -v
  - cargo test
cache:
  cargo: true
  directories:
  - $HOME/openssl-1.1.0h/
  timeout: 360
env:
  global:
  - OPENSSL_DIR=$HOME/openssl-1.1.0h
before_cache:
  - chmod -R a+r $HOME/.cargo
