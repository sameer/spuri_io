sudo: false
language: rust
matrix:
  include:
    - env: TARGET=x86_64-unknown-linux-gnu
      rust: nightly
script:
  - cargo build -v
cache: cargo
before_install:
  - sudo apt-get install -y pkg-config libssl-dev openssl pkg-config
before_cache:
  - chmod -R a+r $HOME/.cargo
